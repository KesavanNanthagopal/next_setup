'use client';  // based on this key it defind that the component is client side componenet which loaded in the browser

export default function Product(){
    return(
        <div>Product list</div>
    )
}